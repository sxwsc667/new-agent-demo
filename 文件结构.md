# 文件结构（File Structure）

> 项目"照片"而非"法律"：如实记录当前每个目录/文件用途，随时能看懂项目长到哪。不约束代码怎么写。

## 使用规则

1. 代码先行，记录跟上：增删改文件后顺手同步本文件，别让照片过期。
2. 诚实优先：只记真实存在的文件，禁止预写未创建内容。
3. 不设禁区：本文件内容全可改，唯一底线是《架构设计》稳定约定不变。

## 目录树

```
study/
├── config/
│   └── api_key.json    # 明文存 DeepSeek、SiliconFlow、lumiya、agnes 密钥与 API 地址，方便使用者填入自己的密钥
├── 架构设计.md          # 项目宪法：给 AI 看的编码规则、核心原则、ADR 决策
├── 文件结构.md          # 本文件：目录结构照片，逐文件标注用途
├── 库管理清单.md        # 库登记册：第三方库及作用，防重复引入
├── 项目记事本.md        # 项目记事本：记录已实现功能与进度
├── .gitignore          # 忽略 .venv、__pycache__、*.pyc、.env
├── .venv/              # Python 3.12.4 虚拟环境，不入库
├── api.py              # LLM API 入口：4 模型对话调用（支持消息历史与工具，多轮对话）+ 流式输出显示与解析
├── box.py              # 全局状态字典 box：主模型、消息历史、调用工具(None 表示本轮未调用)、结束会话标志、回复消息结构占位
├── make_massage.py     # 消息组装：预设初始消息()、用户输入()，直接读写 box["消息"]
├── search_llm.py       # 模型路由：模型映射表 + get_llm_name() + call_llm()
├── llm.py              # 模型调用层：输入消息拼接()（先调 memery.上下文压缩 再拼历史）、use_llm() 调模型写回 box["回复消息"]、返回消息处理()、工具调用写入()
├── tool.py             # 工具调用层：工具判断() 设置 box["是否调用工具"]；工具调用执行() 执行工具并回填结果
├── hooks.py            # 钩子系统：串联 make_massage / llm / tool 模块，标记对话流程节点（会话开始/用户输入/模型调用前后/工具调用前后/工具循环结束/对话结束）
├── chat.py             # 对话循环：ReAct()、deep_think() 两种模式，依赖 box 全局字典判断状态
├── memery.py           # 上下文工程模块：上下文压缩()——当 box["消息"] 累计到 10 轮时，把最老 5 轮压缩成摘要、写进 memory/ 摘要文件，并替换其在消息里的位置（含辅助函数按轮次切分/生成摘要/写入摘要文件）
├── memory/             # 上下文压缩输出的摘要文件目录（单文件 对话摘要.txt，覆盖写）
├── tools/              # 工具目录：工具定义（OpenAI function calling 格式）与工具实现
│   ├── tool_box.py     # 工具实现：查天气(城市)（虚拟返回），供工具调用映射表引用
│   └── tool_list.py    # 工具定义：工具调用映射表 + tool_list（OpenAI function calling 格式）
└── System_Prompt/      # 系统提示词目录：存放对话初始系统提示
    ├── system_prompt.py # 系统提示词：system_prompt（助手身份设定）
    └── skill_list.py   # 技能提示词：skill_list（技能能力说明）
```

## 变更记录

| 日期 | 改动 | 原因 |
| --- | --- | --- |
| 2026-08-25 | memery.py 实现上下文压缩功能；新增 memory/ 摘要输出目录；llm.py 描述更新为输入消息拼接先做上下文压缩 | 用户要求为长对话启用记忆裁剪，避免历史消息无限膨胀 |
| 2026-08-25 | 同步目录树：LLM.py → llm.py、新增 tools/ 与 System_Prompt/、box.py 调用工具说明改 None、make_massage 函数名校正 | AI 核对当前代码与文档不一致，以现有代码为准更新 |
| 2026-08-20 | 建立文档并登记现状 | 初始落盘 |
| 2026-08-20 | 规范文档从 docs/ 平移根目录 | 用户整理，docs 命名不当 |
| 2026-08-20 | 登记 api.py、库管理清单、pyproject.toml、.venv | 接入 openai SDK 并搭虚拟环境 |
| 2026-08-20 | 编码规范并入架构设计，删除 编码规范.md | 压缩要点、集中维护 |
| 2026-08-20 | 新增 项目记事本.md、登记 config/api_key.json | 记录已实现功能；api.py 接入 DeepSeek 密钥配置 |
| 2026-08-20 | api.py 新增 SiliconFlow 调用与流式输出显示函数 | 接入第二家模型（Qwen），实时打印思考过程 |
| 2026-08-20 | 删除 pyproject.toml | 依赖信息以库管理清单为准，脚本项目用不到构建配置 |
| 2026-08-21 | 登记 llm.py、chat.py、memery.py | 新增模型路由、对话入口、记忆模块预留文件 |
| 2026-08-21 | api.py 4 个 xxx_chat 函数新增 消息历史 参数；chat.py 实现多轮对话循环 | 支持连续对话，不再每次只发单轮示例消息 |
| 2026-08-21 | memery.py 实现 组装消息() 和查天气工具；hooks.py 新增 8 个钩子函数；chat.py 改为三种模式循环框架 | 上下文工程基础完成；建立钩子系统；搭建 chat/ReAct/deep_think 三种对话模式的循环结构 |
| 2026-08-21 | 新增 box.py、make_massage.py、search_llm.py、LLM.py；hooks.py/chat.py 改为调用这些模块驱动 ReAct/deep_think 循环 | 引入全局状态字典 box 替代逐层传参，简化模块间状态共享；chat.py 的 chat() 单层模式暂被移除 |