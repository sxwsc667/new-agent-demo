# new-agent 架构设计

> 本文档给 AI 看：指导 AI 如何编码与维护本项目，人不必细读。架构调整先改本文档再改代码；关键决策在文末追加 ADR。
>
> 用户会随时改动项目文件且不记录。AI 的记忆与现有代码冲突时，一律以现有代码为准来维护文档；文档变更记录的时间写「发现时间」（AI 发现不一致的日期），不推测用户实际改动时间。

## 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | `new-agent` |
| 版本 | v0.1.0 |
| 更新 | 2026-08-20 |
| 状态 | 草案 |

## 定位与路线

- **定位**：类 Codex 编程 Agent 桌面软件。Python 后端＝大脑（Agent 内核＋工具），HTML/CSS 前端＝脸（对话界面），接入 MCP 与 Codex 格式 Skill，模型可切换。
- **重启原因**：上一版缺设计变"屎山"，本项目所有架构决策提前写清。
- **路线**：
  - V1（MVP）：对话＋工具调用＋模型切换＋MCP＋Skill。
  - V2：长期记忆、多 Agent 协作、任务队列。
  - V3：同内核换数字人外壳。

## 核心设计原则（违反即打回）

1. **内核与外壳分离**：内核不感知前端技术栈，仅经 API 通信；可无头跑内核方便测试；V3 换脸内核零改动。
2. **统一协议**：工具／MCP Server／Skill 统一为"可调用能力"，Agent 用同一调度逻辑调用。
3. **模型可插拔**：LLM 经统一抽象接口接入，切模型只改配置。
4. **文档先行**：没文档的新模块＝不存在。

## 数据流（一次对话）

   用户 → 前端 UI →(WebSocket)→ 服务层路由 → 会话管理器 → Agent 循环
     → 组装消息历史(记忆+Skill提示词+工具定义) → LLM 适配层
     → 模型返回(文本 或 工具调用) → 工具调度器 → 分发到能力 → 执行
     → 观察结果回填历史 → 循环(直到不再调工具) → 流式推送 → 前端渲染

- **流式输出**：token 实时推送，打字机体验。
- **工具调用是多轮循环**：设 Max Steps 防死循环。
- **用户可见输出全走事件**：内核发事件，前端订阅。

## 技术选型

| 领域 | 选型 | 状态 |
| --- | --- | --- |
| 语言 | Python 3.11+ | ✅ |
| 后端 | FastAPI（异步、内建 WebSocket） | ✅ |
| 前端 | HTML/CSS/JS＋桌面壳（待定） | ⏳ |
| 协议 | HTTP（REST）＋WebSocket | ✅ |
| LLM | OpenAI／Anthropic／本地(Ollama) 经 BaseLLM 抽象 | ✅ |
| MCP | 官方 mcp Python SDK | ✅ |
| 长期记忆 | SQLite＋向量库（待定） | ⏳ |
| 配置 | 配置文件 `config/api_key.json`（密钥明文，见 ADR-0004） | ✅ |
| 测试 | pytest | ✅ |

> ⏳＝待确认，写代码前敲定回填。

## 依赖与边界规则（防屎山）

1. 单向依赖，层间只走公共入口。
2. 接口优先：先定义接口再写实现。
3. 禁止循环依赖。
4. 业务逻辑不进路由（路由文件 ≤ 50 行）。
5. 新模块三件套：职责说明＋头注释＋测试。
6. 密钥明文放 `config/api_key.json`（个人项目，方便填密钥，不做隐藏）；上传/分享前手动处理该文件内容即可。
7. 临时 hack 必须写 `# TODO(临时): 原因＋时间`，否则删掉。

## 编码规范（要点）

> 只保留必须遵守的点。

### 命名

- 新增变量一律用中文；改动时遇到的现有变量保持原名、禁止改名（无论中英文）。

### 注释

- 用中文，先答"为什么"再答"是什么"；复杂逻辑与魔法数字必须注释；注释与代码同步。

### 错误处理

- 边界吞错（只在最外层捕获），`except: pass` 禁止；失败用结构化结果返回。

### 日志

- 用标准库 `logging`，禁止到处 `print`（临时调试打印后续换成前端方法）。

### 其他

- Git：一个提交只做一件事，提交信息写"为什么"。
- 测试：pytest＋Mock，不依赖真实网络/模型。
- 新模块三件套：头注释＋测试＋《文件结构》同步。

## 设计决策记录（ADR）

### ADR-0000 项目重启（2026-08-20）

- 决策：放弃旧项目，新起 `new-agent`，文档先行。
- 理由：旧项目无设计、耦合严重、迭代成本高。
- 备选：重构旧项目（否决：成本≈重写）。

### ADR-0001 目录记录独立到《文件结构》（2026-08-20）

- 决策：目录结构一律在《文件结构》记录，架构设计不重复目录内容。
- 理由：避免两处目录信息漂移，保持单一来源。
- 影响：删除架构设计「目录结构规则」节。

### ADR-0002 命名规范：新增中文、现有不改（2026-08-20）

- 决策：新增变量一律用中文；改动时遇到的现有变量保持原名、禁止改名（无论中英文）。
- 理由：规则简单清晰、无歧义，避免中英混用纠结；不碰已有变量，杜绝改名埋雷。
- 影响：「命名」要点已更新。

### ADR-0003 编码规范压缩并入架构设计（2026-08-20）

- 决策：删除独立《编码规范》文件，压缩为要点并入本文档。
- 理由：细节过多消耗 token；减少文件数量、集中维护。
- 影响：删除 编码规范.md；《文件结构》《库管理清单》同步更新。

### ADR-0004 密钥明文存放、不做隐藏（2026-08-20）

- 决策：API 密钥明文存于 `config/api_key.json`，不加入 .gitignore；使用者直接填入自己的密钥。
- 理由：个人项目，方便使用者填密钥；上传/分享前手动清空即可。
- 影响：密钥约定以此为准，后续不再讨论"是否隐藏密钥"。

### ADR-0005 精简架构设计：删除未实现部分、定位给 AI（2026-08-20）

- 决策：项目定名 `new-agent`；删除「分层架构」「模块职责与禁止事项」「扩展点」三节（尚未实现）；文档主要给 AI 看。
- 理由：未实现内容易漂移、占 token；文档服务 AI 编码，非人读说明书。
- 影响：架构设计大幅精简；现状另见《文件结构》《项目记事本》。

## 待办问题（Open Questions）

| 编号 | 问题 | 拍板人 |
| --- | --- | --- |
| Q1 | 桌面壳用 pywebview 还是 Electron？ | 你 |
| Q2 | 向量库 chromadb 还是 sqlite-vss？ | 你 |

> 悬而未决必须记录，禁止默默跳过。