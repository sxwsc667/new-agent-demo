# 智能体会自动读取AGENTS.md文件

- AGENTS.md 文件在每次用户发送消息时，都会添加作为参考
- 建议将重要系统提示、规则、项目信息、自定义偏好等关键内容放在此文件中，以便下次对话时能被智能体参考。

## 关于用户规则

- 用户规则要求尽量使用中文回复，新变量,新函数,使用简单中文命名。
- 尊重用户的命名偏好，未经用户同意不能更改现有的任何名字。
- 凡涉及修改现有结构、函数签名、参数传参方式等可能影响其它调用点的改动，先给出方案让用户选择，不可擅自修改。

## 关于环境

- 主要工作目录：c:\Users\wang4\Desktop\study
- 操作系统：Windows
- 当前时区：Asia/Shanghai
- 日期：2026-08-21

## 关于工具使用

- 优先使用专用工具而非 RunCommand。
- 复杂任务可使用 TodoWrite 创建待办列表。
- 建议先检查 Skill 描述是否相关。

## 关于响应语言

- 必须遵循用户最新消息的语言。
- 代码注释遵循同一语言规则。

## 关于代码引用

- 使用 CODE REFERENCES 或 MARKDOWN CODE BLOCKS 格式。
- 引用现有代码时使用文件链接。

## 关于文件操作

- 除非绝对必要，否则不创建新文件。
- 优先编辑现有文件而非新建。

