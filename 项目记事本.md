# 项目记事本（Project Notebook）

> 记录项目"已经实现"的功能和当前进度，方便随时回顾。

## 已实现功能

### LLM API 调用（api.py）

- **DeepSeek 对话调用** `deepseek_chat(流式=True)`：
  - 从 `config/api_key.json` 的 `deepseek` 项读取密钥与 API 地址。
  - 调用模型 `deepseek-v4-flash`，系统提示"你是大鲸鱼"，开启思考（thinking）与高推理强度。
  - 按参数 `流式` 切换流式 / 非流式输出。
- **SiliconFlow 对话调用** `SiliconFlow_chat(流式=True)`：
  - 从 `config/api_key.json` 的 `SiliconFlow` 项读取密钥与 API 地址。
  - 调用模型 `Qwen/Qwen3.5-4B`，系统提示"极速助手"，开启思考与高推理强度。
  - 按参数 `流式` 切换流式 / 非流式输出。
- **流式输出显示** `流式输出显示(response)`：
  - 实时打印思考过程（reasoning_content）与回答内容（content）。
  - 思考开始、结束各打印一条分隔线。
- **流式输出解析** `流式输出解析(response)`：
  - 累积思考内容、回答文本，按索引（index）合并工具调用（tool_calls）片段。
  - 返回标准 assistant 消息结构（role / content / tool_calls / reasoning_content 等）。
- **控制台 UTF-8 输出**：规避 Windows 控制台 GBK 编码打印 emoji 时报错。
- **多轮对话支持**：4 个 `xxx_chat` 函数新增 `消息历史` 参数，外部传入完整对话列表即可续接上下文；不传则保持原有单轮示例行为。

### 上下文工程（memery.py）

- **`上下文压缩()`**：当 `box["消息"]` 累计到 **10 轮**（一轮 = 一条 user + 其后到下一 user 的所有消息，含 assistant/tool）时，把最老的 **5 轮**调用当前主模型压成一段简洁中文摘要，替换掉那 5 轮在消息里的位置；最终保留 `system 头部 + 摘要 + 最近保留的若干轮`。
  - 辅助函数：
    - `按轮次切分(消息)`：把消息切分成轮次列表
    - `生成摘要(轮列表)`：调 `get_llm_name(box["主模型"])`，`可用工具=None`，返回摘要文本
    - `写入摘要文件(摘要)`：写到 `memory/对话摘要.txt`（单文件覆盖写，无时间戳）
  - 由 `llm.输入消息拼接()` 在每轮发消息前调用；达到 10 轮触发，压缩后变"摘要 + 最近 5 轮"，再累计到 10 轮会再次滚动压缩。
- **查天气工具**（由 tools/ 目录承担，此处不再重复）

### 全局状态字典（box.py）

- 模块级字典 `box`，作为进程内共享状态，替代模块间逐层传参：
  - `主模型`：当前使用的模型名（对应 `search_llm.模型映射表` 的 key）
  - `消息`：完整对话历史列表（OpenAI messages 格式）
  - `调用工具`：存模型本轮返回的工具调用列表（tool_calls）；初始为 `None`（未调用），有调用时由 `llm.工具调用写入()` 赋值为列表
  - `工具调用结果`：执行工具后的结果列表（role="tool" 的消息）
  - `是否调用工具`：布尔标志，由 `tool.工具判断()` 依据 tool_calls 是否有值设置
  - `结束会话`：布尔标志，标记对话是否应结束
  - `最终回复`：模型本轮最终回答文本（由 `llm.消息追加()` 写入）
  - `回复消息`：模型回复结构占位（role / content / refusal / tool_calls / function_call / audio / annotations / reasoning_content）

### 消息组装（make_massage.py）

- `预设初始消息()`：会话开始时调用，写入初始 system 消息到 `box["消息"]`（内容 = `system_prompt + skill_list`）
- `用户输入()`：每轮模型调用前调用，读取用户输入并追加为 user 消息到 `box["消息"]`；输入"退出"或"111"时置 `box["结束会话"]=True`
- 两个函数都直接 `from box import box` 操作共享字典

### 模型路由（search_llm.py）

- `模型映射表`：模型名 → `api.py` 对应 `xxx_chat` 函数（deepseek / Qwen4b / lumiya / CC_switch）
- `get_llm_name(model_name)`：按名取函数
- `call_llm(model_name, **kwargs)`：统一调用入口

### 模型调用层（llm.py）

- `输入消息拼接()`：发模型前先调 `memery.上下文压缩()`，再把上轮 `box["最终回复"]` 追加为 assistant 消息、`box["工具调用结果"]` extend 为 tool 消息，并设 `box["可用工具"] = tool_list`
- `use_llm()`：取完整消息历史，按 `box["主模型"]` 选模型调用，把回复写回 `box["回复消息"]`
- `返回消息处理()`：从 `box["回复消息"]` 剪出 content（有 tool_calls 时一并带上）组装成 `box["最终回复"]`
- `工具调用写入()`：把 `box["回复消息"]["tool_calls"]` 赋给 `box["调用工具"]`

### 工具调用层（tool.py）

- `工具判断()`：检查 `box["回复消息"]["tool_calls"]` 是否有值，设置 `box["是否调用工具"]`
- `工具调用执行()`：遍历 `box["调用工具"]`，按映射表执行本地工具函数，结果（role="tool"）回填到 `box["工具调用结果"]`

### 钩子系统（hooks.py）

- 串联 `make_massage`、`llm`、`tool` 模块，标记对话流程节点：
  - `会话开始()` → 调 `make_massage.预设初始消息()`
  - `用户输入()` → 调 `make_massage.用户输入()`
  - `模型调用前()` → 调 `llm.输入消息拼接()`
  - `use_model()` → 调 `llm.use_llm()`
  - `模型调用后()` → 调 `llm.返回消息处理()`、`tool.工具判断()`
  - `工具调用前()` → 调 `llm.工具调用写入()`
  - `use_tool()` → 调 `tool.工具调用执行()`
  - `工具调用后()` / `工具循环结束()` / `对话结束()`

### 对话循环（chat.py）

依赖 `box` 全局字典驱动，目前两种模式：

**`ReAct()`**：外层用户输入循环 + 内层工具调用循环
- 流程：`会话开始 → [用户输入 → [模型调用前 → use_model → 模型调用后 → (工具调用前 → use_tool → 工具调用后)*]* ]* → 对话结束`
- 依据 `box["调用工具"]` 是否有值判断是否继续内层循环，直到模型给出最终答案（`工具循环结束()`）

**`deep_think()`**：三层循环（计划拆解 + 分步 ReAct），流程与之前版本一致，目前仍是 TODO 占位框架，未接入 box。

## 备注 / 待办

- 流式输出目前是控制台打印，后续换成前端展示方法。
- 上下文压缩：已在 `memery.py` 实现，由 `llm.输入消息拼接()` 在每轮触发；摘要文件现输出到 `memory/对话摘要.txt`（覆盖写）。阈值 10 轮 / 最老 5 轮当前写在代码里，如需可配置再提出。
- 工具真实调用依赖本地顶层模型路由（`box["主模型"]` 默认 `CC_switch`，走 `api.py` 的 CC_switch_chat，base_url 指向本地 127.0.0.1:15721）；真正跑通需先启动本地路由并核对 `config/api_key.json`。