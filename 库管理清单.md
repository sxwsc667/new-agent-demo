# 库管理清单（Library Registry）

> 记录所有已加载第三方库及用途，防重复引入、重复造轮子。加库前先查这里。

## 使用规则

1. **先查再引**：已有库能实现或项目自己能实现的，一律不新增。
2. **新增流程**：先在"候选与否决"登记评估 → 引入后登记表补行（库名/版本/作用/用在哪/原因/日期）→ 同步《架构设计》、《文件结构》。
3. **诚实优先**：只登记真实已加载（已 import/已安装）的库。
4. **删除同步**：删库同步删登记行，保持登记册＝现状。

## 已加载库登记表

| 库名 | 版本 | 作用 | 用在哪 | 引入原因 | 日期 |
| --- | --- | --- | --- | --- | --- |
| `openai` | 3.3.1 | OpenAI 官方 Python SDK，经 OpenAI 兼容接口调用大语言模型（LLM） | `api.py` | 接入 DeepSeek 与 SiliconFlow(Qwen) 两家模型的底层调用 | 2026-08-20 |
| `requests` | 2.34.2 | 第三方 HTTP 客户端库，发 HTTP 请求、调非 OpenAI 兼容接口 | 暂未使用（已安装，待后续接入） | 已安装留着，后续调其他 HTTP 接口复用 | 2026-08-21 |
| `httpx` | 0.28.1 | 异步 HTTP 客户端，支持 HTTP/2，用于直接调用非 OpenAI 兼容的 REST API（如 Agnes AI） | `api.py` | 原 OpenAI SDK 调 Agnes AI 被 Cloudflare 拦截，改用 httpx 直接发请求绕过 | 2026-08-21 |

> 版本号以本登记表为准，库升级时同步更新此表。

## 候选与否决记录

| 库名 | 当时需求 | 结论 | 理由 |
| --- | --- | --- | --- |
|（暂无）| | | |